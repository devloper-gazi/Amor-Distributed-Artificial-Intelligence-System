"""``tools`` package — host- and container-callable scripts (CLI
entry points, eval runners, judge helpers).

Exists as a regular package so eval runner modules under
``tools.eval`` can be imported by the FastAPI app at startup.
"""
