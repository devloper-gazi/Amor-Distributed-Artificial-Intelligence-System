# Core async and web
asyncio
aiohttp==3.9.1
httpx==0.26.0
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
jinja2==3.1.3
python-multipart==0.0.6

# Message queue
aiokafka==0.10.0

# Databases
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9
motor==3.3.2
pymongo==4.6.1

# Cache
redis[hiredis]==5.0.1
cachetools>=5.3.0  # P1.2: TTLCache for bounded in-memory session storage

# Web scraping
beautifulsoup4==4.12.2
lxml==5.0.0
playwright==1.40.0
trafilatura>=1.6.0
fake-useragent>=1.4.0
robotexclusionrulesparser>=1.7.1

# PDF and Image processing
PyMuPDF==1.23.8
pdfplumber==0.11.0
pytesseract==0.3.10
pdf2image==1.16.3
Pillow==10.2.0
opencv-python-headless>=4.8.0

# Language and translation
fasttext-langdetect==1.0.5
anthropic==0.18.1
ctranslate2>=4.0.0
sentencepiece>=0.2.0

# File processing
pandas==2.1.4
openpyxl==3.1.2
python-docx==1.1.0
xmltodict==0.13.0

# Reliability
tenacity==8.2.3
pybreaker==1.0.2
aiolimiter==1.1.0

# Deduplication
pybloom-live==4.0.0

# RAG and embeddings
sentence-transformers>=2.2.0
lancedb>=0.4.0
numpy>=1.24.0

# Monitoring
prometheus-client==0.19.0
structlog==24.1.0
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-exporter-jaeger==1.21.0

# Progress bars
tqdm==4.66.1

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0

# Code quality
black==24.1.1
flake8==7.0.0
mypy==1.8.0

# Auth & security
argon2-cffi==23.1.0
PyJWT==2.8.0
email-validator==2.1.0

# Code Intelligence — static analysis harness (optional, fail-open)
# All four are imported lazily by static_analysis.py and degrade
# gracefully when missing, so they're safe to keep on the install path.
pylint>=3.2.0
bandit>=1.7.9
radon>=6.0.1

# Code Intelligence v2 — RepoMap + autonomous capability discovery.
# Every entry below is imported lazily and the modules degrade gracefully
# when the import fails, so an offline build still boots.
PyYAML>=6.0          # AdversarialReviewer rule pack
networkx>=3.3        # RepoMap personalized PageRank
huggingface_hub>=0.26.0  # CapabilityDiscoverer HF source
PyGithub>=2.4.0      # CapabilityDiscoverer GitHub source
arxiv>=2.1.3         # CapabilityDiscoverer arXiv source

# Cognitive upgrade — Phase 1A
# Neuro-symbolic verification + RLEF reward collection.
z3-solver>=4.13      # Z3Verifier — SMT termination/overflow/exhaustiveness

# Quick Code V2 — symbolic equivalence check (math tasks only). Opt-in
# via quick_v2_symcode_enabled; the module degrades to a pass-through
# verdict if sympy is absent.
sympy>=1.12          # SymCode — symbolic math equivalence via subprocess
